/* global session $ */
// import {  formatRecordBasedOnSwagger, update } from '../../../helpers/helpers';

export async function formSearchFormDataChange (event) {
  let args = event.args;
  let newValue = args.value;
  let previousValue = args.previousValue;

  console.log(event);
}
